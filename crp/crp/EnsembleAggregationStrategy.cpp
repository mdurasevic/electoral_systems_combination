#include "EnsembleAggregationStrategy.h"

std::vector<double> EnsembleAggregationStrategy::aggregate(std::vector<std::vector<double>>& priorities)
{
	std::vector<double> aggregated;

	aggregated.resize(priorities[0].size(), 0);

	return aggregated;
}
